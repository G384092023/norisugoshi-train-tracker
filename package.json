{
  "name": "norisugoshi-train-tracker",
  "version": "1.0.0",
  "description": "乗り過ごし防止 — train tracker that alerts you before your stop (ODPT data + BLE haptic).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "cap:www": "node sync-www.mjs",
    "cap:sync": "npm run cap:www && npx cap sync android",
    "cap:open": "npm run cap:sync && npx cap open android",
    "cap:run": "npm run cap:www && npx cap run android --list"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@capacitor-community/bluetooth-le": "^8.3.0",
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/local-notifications": "^8.3.1",
    "@capawesome-team/capacitor-android-foreground-service": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
